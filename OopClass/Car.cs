using System;

namespace OopClass
{
    public class Car
    {
        private string color;
        public int maxSpeed;

        // public Car(string _color, int _maxSpeed)
        // {
        //     color = _color;
        //     maxSpeed = _maxSpeed;
        // }
    }
}